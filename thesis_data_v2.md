# CTR 논문 작성 가이드 v2 (제출용 초안)

본 문서는 CTR(광고 클릭률) 분석 프로젝트를 "대회 성능 보고"가 아닌 "논문형 연구"로 정리하기 위한 기준 문서이다. 핵심은 예측 성능 자체보다, CTR 차이를 설명하는 통계적 근거와 그 실무적 함의를 일관되게 제시하는 데 있다.

---

## 1. 연구 방향 (핵심 원칙)

본 연구의 중심 질문은 다음과 같다.

- 어떤 사용자/환경 요인이 CTR 차이를 유의하게 만드는가?
- 해당 요인의 효과는 모델에서도 일관되게 확인되는가?
- 도출된 패턴은 실제 광고 운영 의사결정에 어떻게 연결되는가?

따라서 연구의 주된 흐름은 아래와 같이 구성한다.

1. EDA (기초 분포 및 데이터 특성 점검)
2. 통계 검정 (핵심)
3. 결과 해석 (핵심)
4. 머신러닝 검증 (보조)
5. 실무적 시사점 도출

권장 서술 문장:

> CTR 예측 모델은 통계 분석 결과의 설명력 및 실무적 활용 가능성을 추가적으로 검증하기 위한 보조적 도구로 활용하였다.

---

## 2. 데이터 및 분석 단위

- 학습 데이터: `train.parquet` (전체 10,704,179행)
- 탐색 샘플: `train_sample.parquet` (200,000행, random_state=42, 초기 실험용)
- 검증 샘플: 500,000행 stratified 추출 (`CTR_최종점검.ipynb`, 재현성 점검용)
- 예측 대상: `test.parquet` (1,527,298행)
- 제출 포맷: `sample_submission.csv`

분석 타겟은 이진 변수 `clicked`이며, 전체 CTR은 약 1.91% (클래스 불균형 ~51:1)이다. CTR은 집단별 클릭 비율로 정의한다.

---

## 3. 노트북 역할 분담 (논문 반영 기준)

### 3.1 `CTR_통합_최종.ipynb` (메인 분석)

- RQ 기반 통계 검정 및 해석의 중심 파일
- EDA → ANOVA/카이제곱 통계 검정 → XGBoost + SHAP 순서로 구성
- 논문 본문(방법, 결과, 논의)의 1차 근거로 사용
- 모델은 해석 보조 수준에서 제시

### 3.2 `CTR_최종점검.ipynb` (규모 일반화 검증)

- **핵심 설계**: 동일 파이프라인·동일 XGBoost 하이퍼파라미터로 200k Baseline을 먼저 재학습한 뒤, 500k 결과와 비교 (re-trained baseline) — 샘플 크기 외 변수 완전 통제
- 논문 방법론의 재현성 및 규모 일반화 가능성 주장의 직접 근거로 사용
- 본문 논리를 대체하지 않고 결과 정합성 확인 용도

### 3.3 노트북 구성 요약

| 노트북 | 역할 | 논문 포지션 |
|--------|------|------------|
| `CTR_통합_최종.ipynb` | EDA + 통계 + XGBoost + SHAP 전체 파이프라인 | **핵심 본문** |
| `CTR_최종점검.ipynb` | 200k vs 500k 통제 비교, 재현성 점검 | 방법론 보강 / 부록 |

### 3.4 `CTR_최종점검.ipynb` (최종 검증, 통제 비교)

- 탐색 샘플(200k) 기반 결론이 더 큰 데이터(500k)에서도 재현되는지 점검하는 전용 검증 노트북
- **핵심 설계**: 동일 파이프라인·동일 XGBoost 하이퍼파라미터로 200k Baseline을 먼저 재학습한 뒤, 500k 결과와 비교(re-trained baseline) — 샘플 크기 외 변수 완전 통제
- 논문 방법론의 재현성 및 규모 일반화 가능성 주장의 직접 근거로 사용

---

## 4. 방법론 서술 템플릿 (본문용)

### 4.1 통계 분석 파트

- 집단 간 CTR 차이에 대해 가설을 설정하고 검정을 수행하였다.
- 정규성/분산 가정 충족 여부에 따라 ANOVA 또는 비모수 검정을 적용하였다.
- 범주형 변수 간 연관성은 카이제곱 계열 검정 및 효과 크기로 제시하였다.
- 모든 검정 결과는 p-value와 함께 효과 크기를 병기하였다.

### 4.2 모델 검증 파트

- 통계적으로 유의한 변수 및 상호작용 항을 포함해 예측 모델을 구성하였다.
- 모델 결과는 통계 분석에서 확인된 방향성과의 일치 여부를 중심으로 해석하였다.
- 즉, 모델 성능의 절대값보다 "통계 결과의 재현"을 검증 목표로 설정하였다.

---

## 5. 평가 지표 체계 (논문형)

지표는 과도하게 확장하지 않고 아래 우선순위를 유지한다.

- **메인 지표: LogLoss** (확률 예측 품질) — CTR이 ~1.9%인 강한 클래스 불균형 환경에서 보정 성능 직접 측정
- **보조 지표: AUC** (분류 순위 성능) — 모델의 기본 판별력 평가
- **참고 지표: AP** (Average Precision) — 양성 클래스가 희소한 상황에서 정밀도-재현율 균형 점검
- **부록/각주**: WLL은 "대회 기준 참고 지표"로 간단 보고

> **주의**: 평가 도구(Kaggle Competition) 관점에서는 LogLoss가 제출 지표, 논문 본문에서는 LogLoss + AUC를 함글 제시하는 것을 권장한다.

> **샘플 규모 확대(200k → 500k) 시 주의**: AUC·AP는 개선되나 LogLoss는 상승하는 지표 분리 현상이 관찰된다. 본문에서는 "판별 성능은 향상, 확률 보정 난이도도 증가"로 구분하여 서술한다.

---

## 6. 결과 서술 원칙

### 6.1 지양할 서술

- "어떤 모델이 점수 0.01 더 높다" 중심의 단순 성능 비교

### 6.2 권장 서술

- "어떤 집단에서 CTR이 유의하게 다르며, 그 패턴이 모델에서도 중요한 변수로 재확인되었다"는 구조

예시:

> 20대 집단은 특정 시간대에서 CTR이 유의하게 높았고(p < 0.05), 해당 변수는 예측 모델에서도 상위 설명 변수로 확인되었다.

---

## 7. 논문 본문 연결 구조 (권장)

### 7.1 결과(Results)

1. EDA에서 관찰된 집단 차이 제시
2. 통계 검정 결과(유의성 + 효과 크기) 제시
3. 상호작용 효과(예: 연령×시간대) 해석
4. 모델 결과로 방향성 재확인

### 7.2 논의(Discussion)

- 통계 결과와 모델 결과의 일치/불일치 해석
- 광고 운영 측면 시사점 제시
- 분석 단위, 데이터 기간, 변수 제약 등 한계 명시

### 7.3 결론(Conclusion)

- "예측을 잘함"보다 "CTR 차이의 원인 설명"을 중심으로 요약
- 실무 적용 가능성(타겟팅, 노출 시간 최적화, 예산 배분) 제안

---

## 8. 최종 점검 체크리스트

- [ ] 연구 질문(RQ)별 통계 검정 결과를 모두 제시했는가?
- [ ] p-value와 효과 크기를 함께 보고했는가?
- [ ] 통계 결과와 모델 변수 중요도를 1:1로 연결했는가?
- [ ] 성능 개선보다 해석 중심으로 결론을 작성했는가?
- [ ] 실무적 시사점이 구체적 실행 항목으로 연결되는가?
- [ ] AUC/AP와 LogLoss를 **분리 해석**하여 서술했는가? (규모 확대 시 방향성 차이 명시)
- [ ] Baseline은 hardcoded 값이 아닌 **재학습 기준선(re-trained baseline)**을 사용했는가?
- [ ] 통제 비교 조건(동일 pipeline, 동일 split seed, 동일 hyperparameter)을 명시했는가?

---

## 9. 최종 한 줄 요약

본 연구의 기여는 CTR을 더 잘 맞추는 데 있지 않고, CTR이 왜 달라지는지를 통계적으로 설명하고, 재학습 기준선(re-trained baseline)을 통한 통제 비교로 규모 일반화 가능성까지 실증하여 실무 의사결정으로 연결한 데 있다.

---

## 10. 본문 반영 문안 (결과-논의-결론)

아래 문안은 논문 본문에 바로 반영할 수 있도록 서술형으로 정리한 초안이다.

### 10.1 결과(Results)

본 연구 데이터의 전체 CTR은 약 1.90% 수준으로 확인되어, 타깃 변수 `clicked`가 강한 클래스 불균형 특성을 보였다. 집단 간 CTR 차이를 검정한 결과, 연령, 시간대, 요일 변수에서 통계적 유의성이 확인되었으며, 성별은 통계적으로 유의하지 않았다. 예를 들어 연령 집단의 경우 ANOVA 결과 F=37.1173, p<0.001로 나타났고, 성별 카이제곱 검정에서는 chi-square=1.2350, p=0.2664로 유의하지 않았다. 다만 유의한 변수들의 효과 크기도 연령 eta^2≈0.001296, 성별 Cramer's V≈0.002487, 시간대 eta^2≈0.000289 수준으로 매우 작게 나타나, 통계적 유의성과 실질적 효과를 구분해 해석할 필요가 있음을 확인하였다.

통계 분석 결과의 재현 가능성을 점검하기 위해 XGBoost 기반 보조 검증을 수행하였다. 피처 수 확장 실험에서는 상위 100개 피처를 사용한 설정이 가장 우수했으며, 검증 성능은 AUC 0.6707, AP 0.0492, LogLoss 0.4959로 나타났다. 이는 상위 50개 피처 설정(AUC 0.6437, AP 0.0428, LogLoss 0.5258) 대비 개선된 결과로, 핵심 이력/행동 피처를 지나치게 축소하지 않는 구성이 CTR 예측에 유리함을 시사한다.

결론의 재현성 및 규모 일반화 가능성을 추가로 검증하기 위해 `CTR_최종점검.ipynb`에서 **통제 비교 실험**을 수행하였다. 동일 파이프라인·동일 하이퍼파라미터로 200k Baseline을 재학습한 뒤, 500k Final Check 결과와 비교하여 샘플 크기 외 모든 조건을 통제하였다.

| 구분 | AUC | AP | LogLoss |
|---|---|---|---|
| Baseline (200k, 재학습) | 0.7017 | 0.0599 | 0.4640 |
| Final Check (500k) | 0.7294 | 0.0742 | 0.5314 |
| **Δ (변화량)** | **+0.0276** | **+0.0143** | **+0.0675** |

샘플 규모 확대로 AUC·AP는 향상되었으나, LogLoss는 상승하였다. 이는 학습 데이터 다양성 증가에 따른 확률 보정 난이도 상승으로 해석된다.

모델 해석을 위한 SHAP 분석에서는 `history_a_1`, `feat_d_4`, `feat_e_3`가 상위 설명 변수로 도출되었다. 이 중 `history_a_1`과 `feat_d_4`는 값 증가 시 예측 CTR 상승 방향, `feat_e_3`는 예측 CTR 하락 방향으로 작용하였다. 즉, CTR은 단일 인구통계 변수보다 과거 반응 이력과 세부 행동/속성 피처의 결합에 의해 설명되는 경향이 강하게 관찰되었다.

최종 점검(500k)의 피처 중요도 상위 변수는 `inventory_id_2`, `history_a_1`, `l_feat_16`, `inventory_id_37`, `history_a_2` 등으로 나타났으며, 기존 핵심 변수군(`history_a_1`, `history_b_2`)과의 중복이 확인되었다. 이는 변수 중요도의 전반적 구조가 샘플 규모 확대 후에도 완전히 역전되지 않음을 시사한다.

### 10.2 논의(Discussion)

본 연구 결과는 CTR 차이에 대한 통계적 유의성이 존재하더라도, 효과 크기가 작을 경우 운영 단계의 기대효과는 제한적일 수 있음을 보여준다. 이는 대규모 표본에서 p-value가 작게 나타나는 현상과도 일치하며, 실무 해석에서는 유의성뿐 아니라 효과 크기를 함께 보고하는 접근이 필요하다.

또한 통계적으로 확인된 변수 패턴이 모델 해석(SHAP/피처 중요도)에서도 유사한 방향으로 재현되었다는 점에서, 분석 프레임의 일관성과 설명 가능성이 확보되었다. 특히 history 계열 변수의 높은 중요도는 사용자의 과거 반응 정보가 현재 클릭 가능성 설명에 핵심적인 단서임을, 탐색 샘플(200k)과 검증 샘플(500k) 양쪽에서 일관되게 확인하였다.

**규모 일반화 검증**: 동일 조건 통제 비교(re-trained baseline) 결과, Baseline(200k) AUC 0.7017 대비 Final Check(500k) AUC 0.7294(Δ+0.0276)로 판별 성능이 향상되었다. 단, LogLoss는 0.4640 → 0.5314(Δ+0.0675)로 상승하였으며, 이는 학습 패턴 다양화에 따른 보정 난이도 증가로 설명할 수 있다. AUC/AP와 LogLoss의 방향성 차이는 논문에서 각 지표의 의미를 분리하여 명시적으로 서술할 것.

따라서 본 연구의 주된 기여는 최고 성능 달성 자체가 아니라, CTR 변동 요인을 통계적으로 검정하고 이를 머신러닝 해석으로 연결하며, 재학습 기준선을 사용한 통제 비교로 규모 일반화 가능성까지 실증한 데 있다.

### 10.3 결론(Conclusion)

종합하면, 본 연구는 CTR 변동이 인구통계학적 특성 단독보다 반응 이력 및 행동/속성 피처의 조합에 의해 더 강하게 설명됨을 확인하였다. 통계 검정과 모델 해석이 일관되게 연결되었으며, 동일 조건 통제 비교(re-trained baseline)를 통해 샘플 규모 확대 효과를 독립적으로 측정함으로써 결과의 재현성과 규모 일반화 가능성을 추가로 확인하였다.

판별 성능(AUC·AP)은 샘플 규모 확대에 따라 안정적으로 향상되었으며, 보정 성능(LogLoss) 변화는 데이터 다양성 증가의 부산물로 해석된다. 실무적으로는 연령/성별 중심의 단순 세분화보다 반응 이력 기반 세그먼트 운영, 노출 빈도 및 타이밍 최적화 전략이 더 효과적일 가능성이 높다.
